import React from "react";
import PropTypes from "prop-types";
import Style from "./Row.module.scss";

const Row = ({ element }) => {
  console.log(element);
  return <div>Row</div>;
};

Row.propTypes = {};

export default Row;
